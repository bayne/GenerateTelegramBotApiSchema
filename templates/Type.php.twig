<?php

namespace {{ __TYPES_NAMESPACE }};

class {{ type.name }} extends {{ type.parent }}
{
{% for field in type.fields %}
    /**
     * {{ type.link }}
     *
     * {{ field.description|striptags }}
     *
     * @var {{ field.type }}
    {%- if field.required == false -%}
       |null
    {%- endif %}

     */
    protected ${{ field.name|camelize }};

{% endfor %}

{% for field in type.fields %}
    /**
     * @param {{ field.type }} ${{ field.name|camelize }}
     * @return static
     */
    public function {{ ('set_' ~ field.name)|camelize }}(
    {%- if field.is_multiple_types == false -%}
        {%- if field.is_collection %}array {% else %}{{ field.type }} {% endif -%}
    {%- endif -%}
    ${{ field.name|camelize }}): self
    {
        $this->{{ field.name|camelize }} = ${{ field.name|camelize }};

        return $this;
    }

    /**
     * @return {{ field.type }}
    {%- if field.required == false -%}
        |null
    {%- endif %}

     */
    public function {{ ('get_' ~ field.name)|camelize }}()
     {%- if field.is_multiple_types == false -%}: {% if field.required == false -%}
             ?
         {%- endif -%}
         {%- if field.is_collection -%}
             array
         {%- else -%}
             {{ field.type }}
         {%- endif -%}
    {%- endif %}

    {
        return $this->{{ field.name|camelize }};
    }

{% endfor %}
}